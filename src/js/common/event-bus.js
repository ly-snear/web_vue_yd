import Vue from 'vue';

/**
 * 导出事件总线
 * @type {Vue | CombinedVueInstance<Vue, object, object, object, Record<never, any>, {}, ComponentOptionsMixin, ComponentOptionsMixin>}
 */
export const EventBus = new Vue();
